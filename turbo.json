{
	"$schema": "https://turbo.build/schema.json",
	"globalDependencies": ["**/.env.*local"],
	"tasks": {
		"build": {
			"outputs": ["dist/**", ".astro/**"]
		},
		"lint": {},
		"format": {},
		"dev": {
			"cache": false,
			"persistent": true
		}
	}
}
